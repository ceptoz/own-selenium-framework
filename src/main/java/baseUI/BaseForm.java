package baseUI;

import org.openqa.selenium.By;

public abstract class BaseForm {
//    By uniqueFormLocator;
//    String formName;
//    public BaseForm (By locator, String name) {
//        uniqueFormLocator = locator;
//        formName = name;
//    }
//    public boolean isFormOpen() {
//        LoggerUtil.debugLog("Checking if the page object is open ", formName);
//        return Browser.getWait().waitForVisibility(uniqueFormLocator).isDisplayed();
//    }
//    public boolean isFormClosed() {
//        LoggerUtil.debugLog("Checking if the page object is closed", formName);
//        return Browser.getWait().waitForHide(uniqueFormLocator);
//    }
}
